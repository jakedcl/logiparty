# Database (Neon PostgreSQL)
DATABASE_URL=postgresql://user:password@host/db?sslmode=require

# NextAuth — generate: openssl rand -base64 32
AUTH_SECRET=
AUTH_URL=http://test.localhost:3000
AUTH_TRUST_HOST=true

# Subdomain tenancy
NEXT_PUBLIC_ROOT_DOMAIN=logiparty.com
# If set, bare localhost acts as this tenant (use test.localhost for tenant
# and leave this unset/empty to preview the apex marketing homepage).
# NEXT_PUBLIC_DEV_ORG_SLUG=test

# Cloudflare R2 (M4+)
# R2_ACCOUNT_ID=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET_NAME=
# R2_PUBLIC_URL=

# Resend (M1+)
# RESEND_API_KEY=
# RESEND_FROM_EMAIL=ops@yourdomain.com
# Optional: where marketing lead form emails go (A3)
# LEADS_NOTIFY_EMAIL=you@example.com

# Optional cron auth for /api/cron/auto-ready
# CRON_SECRET=
